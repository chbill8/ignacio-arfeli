# Before / after clips

Drop two files here and the player on the site switches to them automatically:

- `before.mp3` — the unmastered mix
- `after.mp3`  — the delivered master

Both should be the same section of the same track, starting at the same point,
20–40 seconds long, MP3 (320 kbps) or AAC. No processing is applied on the site.
